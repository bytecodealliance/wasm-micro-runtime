[pytest]
markers =
    multi_sdk: parametrize over all detected wasi-sdk-*-x86_64-linux installations under /opt
    slow: long-running test (typically a build + run); excluded by default if running -m "not slow"
testpaths = .
